{
  "buildCommand": "npx nx build client --configuration=production && cp -r dist/apps/client/browser/* dist/apps/client/",
  "outputDirectory": "dist/apps/client",
  "installCommand": "pnpm i",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}